import AuthCard from "@/components/auth/AuthCard";

export const metadata = {
    title: "Login — ZENJI",
    description: "Sign in or create your ZENJI account.",
};

export default function LoginPage() {
    return <AuthCard />;
}